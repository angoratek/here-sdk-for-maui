# Build outputs
bin/
obj/
artifacts/
# MSBuild binary logs (re-generate with `dotnet build -bl`)
*.binlog

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
settings.json

# .NET
*.swp
*.*~
project.lock.json
project.fragment.lock.json
artifacts/

# NuGet
**/packages/*
*.nupkg
**/[Pp]ackages/**

# macOS
.DS_Store
._*

# SDK archives (too large for git)
tmp/

# Test artifacts (TRX logs, captured screenshots)
**/TestResults/
**/screenshots/

# HERE SDK binaries (AAR ~61MB, JARs) — restore from tmp/ or download
*.aar
*.jar

# iOS build outputs
*.xcuserdata/
*.xcscmblueprint
build/
DerivedData/

# Android
*.apk
*.aab
local.properties

#iOS
Libs

# Secrets
*.env
credentials.json
# Local override of appsettings.json — keep real HERE SDK credentials here, NOT in the committed file.
appsettings.Local.json