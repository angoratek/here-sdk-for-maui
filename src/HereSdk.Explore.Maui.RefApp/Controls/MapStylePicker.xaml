<?xml version="1.0" encoding="UTF-8" ?>
<Border xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:local="clr-namespace:Here.Explore.Maui.RefApp.Controls"
        x:Class="Here.Explore.Maui.RefApp.Controls.MapStylePicker"
        x:Name="This"
        VerticalOptions="End"
        HorizontalOptions="End"
        Margin="0,0,16,16"
        StrokeShape="RoundRectangle 16"
        Background="Transparent"
        InputTransparent="false">

    <Grid RowDefinitions="Auto,Auto" MinimumWidthRequest="48" InputTransparent="false">
        <!-- Dropdown panel -->
        <Border Grid.Row="0"
                x:Name="DropdownPanel"
                StrokeShape="RoundRectangle 16"
                BackgroundColor="{AppThemeBinding Light=White, Dark=#2C2C2E}"
                IsVisible="{Binding IsDropdownExpanded, Source={x:Reference This}}"
                Margin="0,0,0,8"
                Padding="8"
                InputTransparent="false">
            <Border.Shadow>
                <Shadow Brush="{AppThemeBinding Light=#60000000, Dark=#90000000}" Offset="0,4" Radius="16" />
            </Border.Shadow>

            <VerticalStackLayout Spacing="4" InputTransparent="false">
                <Label Text="Map Style"
                       FontSize="11"
                       FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light=#888888, Dark=#AEAEB2}"
                       HorizontalOptions="Center"
                       Margin="0,4,0,4" />

                <VerticalStackLayout Spacing="2"
                                     BindableLayout.ItemsSource="{Binding MapSchemes, Source={x:Reference This}}"
                                     InputTransparent="false">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="local:MapSchemeItem">
                            <Grid Padding="8,8" InputTransparent="false">
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding SelectSchemeCommand, Source={x:Reference This}}"
                                                          CommandParameter="{Binding .}" />
                                </Grid.GestureRecognizers>
                                <Grid ColumnDefinitions="28,*">
                                    <Label Grid.Column="0"
                                           Text="{Binding Icon}"
                                           FontSize="16"
                                           VerticalOptions="Center"
                                           HorizontalOptions="Center" />
                                    <Label Grid.Column="1"
                                           Text="{Binding Label}"
                                           FontSize="13"
                                           TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
                                           VerticalOptions="Center" />
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </Border>

        <!-- Toggle button -->
        <Button Grid.Row="1"
                x:Name="ToggleButton"
                Text="🗺️"
                WidthRequest="48"
                HeightRequest="48"
                CornerRadius="24"
                Command="{Binding ToggleDropdownCommand}"
                FontSize="20"
                Padding="0"
                BackgroundColor="{AppThemeBinding Light=White, Dark=#2C2C2E}">
            <Button.Shadow>
                <Shadow Brush="{AppThemeBinding Light=#50000000, Dark=#90000000}" Offset="0,4" Radius="12" />
            </Button.Shadow>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Pressed">
                            <VisualState.Setters>
                                <Setter Property="Scale" Value="0.95" />
                                <Setter Property="Opacity" Value="0.7" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </VisualStateManager.VisualStateGroups>
        </Button>
    </Grid>
</Border>
